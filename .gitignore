# Secrets and credentials — never commit
.env
.env.local
.env.*.local
!.env.example
twinbox.json
*.pem
*.key
id_rsa*
id_ed25519*
credentials.json
secrets.json
himalaya.toml
**/himalaya/config.toml

# macOS / editor junk
.DS_Store
.idea/
*.swp
*~

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml
.worktrees/
build/

# Node
node_modules/

# Local Twinbox state, runtime mail dumps, and temp data (PII)
.twinbox
.runtime/
runtime/
logs/
.logs/
*.log
tmp/
*.db
*.sqlite
*.sqlite3
*.eml
*.mbox
activity-pulse.json
state.json

# Local leftover stack / machine-specific paths (not part of openclaw-skill)
/bin/
/packages/
/src/
/docs/
/scripts/

# Local scratch notes (may contain machine paths, links, or secrets)
/issue.md
/INSTALL_GUIDE.md
/README_REFACTOR.md
/REFACTOR_COMPLETE_FINAL.md
/REFACTOR_FINAL.md
/VERIFICATION_COMPLETE.md

.beads/*
!.beads/formulas/
.beads/formulas/*
!.beads/formulas/twinbox-*.formula.toml
.claude/
!.claude/skills/

# Local user materials and office documents
*.xlsx
*.xls
*.docx
*.doc
*.pptx
*.ppt
*.pdf

# Instance-specific validation artifacts (under repo docs/ or state root); do not commit
/docs/validation/
claude-agent-sdk-demos
gastown
# Local symlink to ~/openclaw-docker-compose (machine-specific; may contain secrets)
openclaw-docker-compose
# Dolt database files (added by bd init)
.dolt/

# OpenClaw plugin (install deps locally; lockfile is committed slim)
integrations/openclaw/plugin-twinbox-task/node_modules/

# Local Twinbox state root, vendored unpack, and Go build output (see AGENTS.md)
vendor/
dist/

# Host-specific OpenClaw JSON fragment (copy from openclaw.fragment.example.json)
integrations/openclaw/openclaw.fragment.json
