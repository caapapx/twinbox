{
  "id": "twinbox",
  "name": "Twinbox",
  "description": "Email intelligence skill — read-only IMAP, thread analysis, urgent/pending/weekly output.",
  "version": "0.1.0",
  "kind": "tool",
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "properties": {
      "cwd": {
        "type": "string",
        "description": "Path to twinbox_core source root"
      }
    }
  }
}
