# Agent Instructions（入口，勿在此维护正文）

本仓库怎么干活只维护一份：[CLAUDE.md](./CLAUDE.md)。

@CLAUDE.md

产品 MCP 用法：仓内 `twinbox` skill（`.cursor/skills/twinbox`、`.codex/skills/twinbox`）。热更本机 `twinbox-release-remote`；the site host `twinbox-release-local`（尚未建则仍走 remote 文档，勿把 local 拷到 Mac）。
