{
  "id": "twinbox-task-tools",
  "name": "Twinbox task tools",
  "description": "Registers deterministic agent tools that run twinbox task subcommands (read-only JSON).",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "twinboxBin": {
        "type": "string",
        "default": "twinbox",
        "description": "twinbox executable name or absolute path on the Gateway host"
      },
      "cwd": {
        "type": "string",
        "description": "Working directory (Twinbox code root). Falls back to TWINBOX_CODE_ROOT env if unset."
      }
    }
  }
}
